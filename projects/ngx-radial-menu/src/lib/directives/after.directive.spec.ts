import {AfterDirective} from './after.directive';

describe('AfterDirective', () => {
  it('should create an instance', () => {
    // const directive = new AfterDirective();
    // expect(directive).toBeTruthy();
  });
});
